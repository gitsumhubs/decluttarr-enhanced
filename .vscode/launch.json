{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Python: Main from Root",
        "type": "python",
        "request": "launch",
        "program": "${workspaceFolder}/main.py",
        "cwd": "${workspaceFolder}",
        "env": {
          "PYTHONPATH": "${workspaceFolder}"
        }
      }
    ]
  }